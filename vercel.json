{
  "buildCommand": "npx expo export --platform web || echo 'Using prebuilt dist'",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
